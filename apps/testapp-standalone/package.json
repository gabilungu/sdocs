{
	"name": "testapp-standalone",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"description": "Test project for the standalone sdocs CLI — sdocs is intentionally not a dependency",
	"devDependencies": {
		"clsx": "^2.1.1",
		"svelte": "^5.55.2"
	}
}
