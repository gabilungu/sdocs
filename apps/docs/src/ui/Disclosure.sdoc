<script lang="ts">
	import Disclosure from './Disclosure.svelte';
</script>

[SHOWCASE title="@demo/Disclosure" description="Expandable panel with a children snippet, a parameterized marker snippet, and an ontoggle event."]

	[COMPONENT component={Disclosure} status="draft" args={{ summary: 'What is a .sdoc file?', open: false }}]
		<Disclosure {...args}>
			A file of <code>[SHOWCASE]</code>, <code>[DOC]</code>, and <code>[LAYOUT]</code>
			blocks, rendered by the Explorer with live previews and controls.
		</Disclosure>
	[/COMPONENT]

	[EXAMPLE title="StartsOpen"]
		<Disclosure {...args} summary="Already expanded" open>
			The <code>open</code> prop only sets the initial state — the component
			manages its own from there.
		</Disclosure>
	[/EXAMPLE]

	[EXAMPLE title="CustomMarker"]
		<Disclosure {...args} summary="Custom marker snippet">
			{#snippet marker(open)}
				<span>{open ? '−' : '+'}</span>
			{/snippet}
			The marker snippet receives the open state as a parameter.
		</Disclosure>
	[/EXAMPLE]

[/SHOWCASE]
