<script lang="ts">
	import Notice from './Notice.svelte';
</script>

[SHOWCASE
	title="@demo/Notice"
	description="Kitchen sink: every documented feature in one component — text, number, boolean, and union props; events; plain and parameterized snippets; methods; exported state; and annotated CSS custom properties."
]

	[NOTES]
		- ready: The kitchen sink — this page exists to exercise every documented feature at once, not to model a real component doc.
		- Also the corpus the language-server and formatter tests run against.
	[/NOTES]

	[COMPONENT component={Notice} synonyms="callout, alert, admonition, banner" args={{ title: 'Build finished', tone: 'success', dismissible: true, count: 1 }}]
		<Notice {...args}>
			All 12 pages compiled in 1.2s — try dismissing me, or change my props
			in the table below.
		</Notice>
	[/COMPONENT]

	[EXAMPLE title="WithIconSnippet" tags="icon, snippet, warning"]
		<Notice {...args} title="Deploy warning" tone="warning">
			{#snippet icon(tone)}
				<span>{tone === 'warning' ? '⚠️' : 'ℹ️'}</span>
			{/snippet}
			The icon snippet receives the current tone as a parameter.
		</Notice>
	[/EXAMPLE]

	[EXAMPLE title="Repeated" tags="count, badge"]
		[NOTES]
			- bug: The count badge has no upper bound yet — past 99 it widens the title row.
		[/NOTES]
		<Notice {...args} title="Connection retried" tone="danger" count={4}>
			The count prop renders an occurrence badge next to the title.
		</Notice>
	[/EXAMPLE]

	[EXAMPLE title="WithAction"]
		<Notice {...args} title="Update available" action={{ label: 'Restart now' }}>
			The action prop is a custom object type — see how it appears in the
			Props table.
		</Notice>
	[/EXAMPLE]

	[EXAMPLE title="Themed"]
		<div style="--notice-accent: #fc1d29; --notice-bg: #fff5f5; --notice-radius: 4px;">
			<Notice {...args} title="Mascot mode">
				Styled entirely through the annotated CSS custom properties.
			</Notice>
		</div>
	[/EXAMPLE]

[/SHOWCASE]
