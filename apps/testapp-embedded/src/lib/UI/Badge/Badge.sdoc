<script>
	import Badge from './Badge.svelte';
</script>

[SHOWCASE title="UI / Badge" notes={[{ note: 'Documented through JSDoc rather than TypeScript.', intent: 'success' }]} description="A plain-JS badge documented through JSDoc."]

	[component component={Badge} args={{ label: 'New', tone: 'info', pill: false }}]
		<Badge {...args} />
	[/component]

	[example title="Success" notes={[{ note: 'This example is here to fail loudly if the tone colours regress.', intent: 'danger' }]}]
		<Badge label="Done" tone="success" />
	[/example]

	[example title="Dismissable"]
		<Badge label="Closable" tone="warning" ondismiss={() => alert('dismissed')} />
	[/example]

[/SHOWCASE]
