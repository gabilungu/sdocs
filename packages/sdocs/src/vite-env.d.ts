// Vite's module declarations for asset imports (`*.svg?raw`, css side-effect
// imports) — the Explorer sources build under Vite. Lives outside src/lib so
// it types the check without shipping a vite/client reference in the package.
/// <reference types="vite/client" />
