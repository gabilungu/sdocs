<script>
	export const meta = {
		title: ':Pages / About',
		description: 'What this project tests.',
	};
</script>

<div style="max-width: 640px; padding: 32px;">
	<h1>standalone</h1>
	<p>
		This project documents components with sdocs <strong>without installing
		it</strong> — the docs server runs via <code>npx sdocs run</code> (or the
		local CLI from the monorepo).
	</p>

	<h2>What it covers</h2>
	<ul>
		<li>The no-install CLI flow</li>
		<li>Host-only dependencies (<code>clsx</code>) resolving from this project</li>
		<li>Plain-JS components documented via JSDoc</li>
	</ul>
</div>
