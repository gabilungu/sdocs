<script lang="ts">
	import Disclosure from './Disclosure.svelte';
</script>

[DOCS title="Disclosure" description="Expandable panel with a children snippet, a parameterized marker snippet, and an ontoggle event."]

	[preview component={Disclosure} args={{ summary: 'What is a .sdoc file?', open: false }}]
		<Disclosure {...args}>
			A Svelte file with an <code>export const meta</code> — one per component,
			rendered by the Explorer with live previews and controls.
		</Disclosure>
	[/preview]

	[example title="StartsOpen"]
		<Disclosure {...args} summary="Already expanded" open>
			The <code>open</code> prop only sets the initial state — the component
			manages its own from there.
		</Disclosure>
	[/example]

	[example title="CustomMarker"]
		<Disclosure {...args} summary="Custom marker snippet">
			{#snippet marker(open)}
				<span>{open ? '−' : '+'}</span>
			{/snippet}
			The marker snippet receives the open state as a parameter.
		</Disclosure>
	[/example]

[/DOCS]
